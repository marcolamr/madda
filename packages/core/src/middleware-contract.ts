/** Extension point for global pipeline, route groups, etc. */
// eslint-disable-next-line @typescript-eslint/no-empty-object-type -- surface grows with the framework
export interface MiddlewareContract {}
