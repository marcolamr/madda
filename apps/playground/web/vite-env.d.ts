/// <reference types="vite/client" />

/** Injetado por `@madda/play-web` no `createViteServer` (SSR + client). */
declare const __PLAY_WEB_INTERNAL_ORIGIN__: string;
declare const __PLAY_APP_LOCALE__: string;
